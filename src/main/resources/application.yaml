spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bookingsystem?useSSL=false&serverTimezone=UTC
    username: root
    password: Lbr050429
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true